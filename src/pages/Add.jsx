import React from "react";
import AddContainer from "../containers/AddContainer";

const Add = () => {
    return (
        <div>
            <AddContainer />
        </div>
    );
};

export default Add;
